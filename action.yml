name: 'Secret Scanning Review Action'
author: "@felickz"
description: 'Increase the visibility of Secret Scanning alerts as part of pull request collaboration'
inputs:
  token:
    description: 'GitHub Access Token that provides access to the repository. Must be provided via either parameter or environment variable. The github.token cannot be used as it does not provide secret scanning alert access.'
    required: false #TODO: make this required once the feature is GA (breaking change vs initial env var approach)
  fail-on-alert:
    description: 'A boolean to raise workflow/annotation error(instead of warning) if alert is detected.'
    required: false
    default: 'false'
  fail-on-alert-exclude-closed:
    description: "A boolean to exclude an alert from FailOnAlert if it is marked as closed (state: 'resolved')."
    required: false
    default: 'false'
  disable-pr-comment:
    description: 'A boolean to disable the PR comment feature.'
    required: false
    default: 'false'
  runtime:
    description: 'The runtime to use for the action (`powershell` or `python`).'
    required: false
    default: 'powershell'
  python-http-proxy-url:
    description: 'The URL of the http proxy to use for GitHub API requests (python runtime only).'
    required: false
    default: ''
  python-https-proxy-url:
    description: 'The URL of the https proxy to use for GitHub API requests (python runtime only).'
    required: false
    default: ''
  python-verify-ssl:
    description: 'A boolean to enable or disable SSL verification for GitHub API requests (python runtime only). NOTE: This is NOT recommended and may introduce additional security risks.'
    required: false
    default: 'true'
  skip-closed-alerts:
    description: 'A boolean to enable or disable skipping closed alerts from evaluation.'
    required: false
    default: 'false'
  python-skip-closed-alerts:
    description: '[DEPRECATED - Use skip-closed-alerts instead] A boolean to enable or disable skipping closed alerts from evaluation (python runtime only).'
    required: false
    default: 'false'
  disable-workflow-summary:
    description: 'A boolean to disable the workflow summary comment.'
    required: false
    default: 'false'
  python-disable-workflow-summary:
    description: '[DEPRECATED - Use disable-workflow-summary instead] A boolean to disable the workflow summary comment (python runtime only).'
    required: false
    default: 'false'
outputs:
  alerts:
    description: 'Details about the alerts detected in the PR.'
    value: ${{ steps.python-run.outputs.alerts || steps.powershell-run.outputs.alerts }}
runs:
    using: composite
    steps:
      - name: Warn on deprecated python-skip-closed-alerts
        if: ${{ inputs.python-skip-closed-alerts == 'true' }}
        run: echo "::warning::The 'python-skip-closed-alerts' input is deprecated and will be removed in v3. Please use 'skip-closed-alerts' instead."
        shell: bash
      - name: Warn on deprecated python-disable-workflow-summary
        if: ${{ inputs.python-disable-workflow-summary == 'true' }}
        run: echo "::warning::The 'python-disable-workflow-summary' input is deprecated and will be removed in v3. Please use 'disable-workflow-summary' instead."
        shell: bash
      - name: Run Powershell Script
        if: ${{ inputs.runtime == 'powershell' }}
        id: powershell-run
        run: |
          & "$env:ACTION_PATH/action.ps1" `
            -GitHubToken $env:GITHUB_TOKEN `
            -FailOnAlert ($env:INPUT_FAIL_ON_ALERT -eq 'true') `
            -FailOnAlertExcludeClosed ($env:INPUT_FAIL_ON_ALERT_EXCLUDE_CLOSED -eq 'true') `
            -DisablePRComment ($env:INPUT_DISABLE_PR_COMMENT -eq 'true') `
            -DisableWorkflowSummary ($env:INPUT_DISABLE_WORKFLOW_SUMMARY -eq 'true') `
            -SkipClosedAlerts ($env:INPUT_SKIP_CLOSED_ALERTS -eq 'true')
        shell: pwsh
        env:
          ACTION_PATH: ${{ github.action_path }}
          GITHUB_TOKEN: ${{ inputs.token }}
          INPUT_FAIL_ON_ALERT: ${{ inputs.fail-on-alert }}
          INPUT_FAIL_ON_ALERT_EXCLUDE_CLOSED: ${{ inputs.fail-on-alert-exclude-closed }}
          INPUT_DISABLE_PR_COMMENT: ${{ inputs.disable-pr-comment }}
          INPUT_DISABLE_WORKFLOW_SUMMARY: ${{ inputs.disable-workflow-summary }}
          INPUT_SKIP_CLOSED_ALERTS: ${{ inputs.skip-closed-alerts || inputs.python-skip-closed-alerts }}
      - name: Run Python Script
        if: ${{ inputs.runtime == 'python' }}
        id: python-run
        run: |
          pip install -r "$ACTION_PATH/requirements.txt" --require-hashes
          python "$ACTION_PATH/action.py" \
            --FailOnAlert "$INPUT_FAIL_ON_ALERT" \
            --FailOnAlertExcludeClosed "$INPUT_FAIL_ON_ALERT_EXCLUDE_CLOSED" \
            --DisablePRComment "$INPUT_DISABLE_PR_COMMENT" \
            --ProxyURLHTTP "$INPUT_PROXY_HTTP" \
            --ProxyURLHTTPS "$INPUT_PROXY_HTTPS" \
            --VerifySSL "$INPUT_VERIFY_SSL" \
            --SkipClosedAlerts "$INPUT_SKIP_CLOSED_ALERTS" \
            --DisableWorkflowSummary "$INPUT_DISABLE_WORKFLOW_SUMMARY"
        shell: bash
        env:
          ACTION_PATH: ${{ github.action_path }}
          GITHUB_TOKEN: ${{ inputs.token }}
          INPUT_FAIL_ON_ALERT: ${{ inputs.fail-on-alert }}
          INPUT_FAIL_ON_ALERT_EXCLUDE_CLOSED: ${{ inputs.fail-on-alert-exclude-closed }}
          INPUT_DISABLE_PR_COMMENT: ${{ inputs.disable-pr-comment }}
          INPUT_PROXY_HTTP: ${{ inputs.python-http-proxy-url }}
          INPUT_PROXY_HTTPS: ${{ inputs.python-https-proxy-url }}
          INPUT_VERIFY_SSL: ${{ inputs.python-verify-ssl }}
          INPUT_SKIP_CLOSED_ALERTS: ${{ inputs.skip-closed-alerts || inputs.python-skip-closed-alerts }}
          INPUT_DISABLE_WORKFLOW_SUMMARY: ${{ inputs.disable-workflow-summary || inputs.python-disable-workflow-summary }}
branding:
  icon: "lock"
  color: "gray-dark"
